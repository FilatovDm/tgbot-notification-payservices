services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tgbot-notification-payservices
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TZ: Europe/Moscow
    volumes:
      - ./subscriptions.db:/app/subscriptions.db
